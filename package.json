{
  "name": "docker-nodejs",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/bin/www",
    "heroku-postbuild": "npm i --prefix=src && mv src/node_modules /app/"
  },
  "engines": {
    "node": "12.x"
  }
}
